using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class pointer : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void pos1()
    {
        transform.localPosition = new Vector3(-135, 20, 0);
    }
    public void pos2()
    {
        transform.localPosition = new Vector3(-99, -24, 0);
    }
    public void pos3()
    {
        transform.localPosition = new Vector3(-47, -72, 0);
    }
    public void pos4()
    {
        transform.localPosition = new Vector3(-47, -116, 0);
    }
}
